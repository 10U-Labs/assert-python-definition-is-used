# Commit straight to main

Commit to `main` and push. Do not branch and do not offer a pull request:
`has_pull_requests` is false for this repository, so a pull request cannot
be opened and a topic branch is a dead end.
