# Verify platform capabilities against the API

Before saying a platform cannot do something, ask its API or CLI and quote
the field that answers. If no field answers, say the response did not
mention it, not that the thing does not exist.
